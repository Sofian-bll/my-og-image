services:
  og-image:
    build:
      dockerfile: Dockerfile.dev
    container_name: og-image-dev
    restart: unless-stopped
    ports:
      - "3042:3000"
    volumes:
      - .:/app
      - /app/node_modules
